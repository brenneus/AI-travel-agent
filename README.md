# AI-travel-agent
An AI Agent that can plan and book a vacation.

## To Use

1. **Install dependencies and browsers:**
   Run the following commands in your terminal to install the required Python libraries and the Playwright browser engines:

   ```bash
   pip install -r requirements.txt
   playwright install